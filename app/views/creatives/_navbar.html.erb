<nav id="mainNav" class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!--<a class="navbar-brand page-scroll" href="#page-top">Savecation</a>-->
      <%= link_to "Savecation", root_url, :class => "navbar-brand page-scroll" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "Home", root_path %>
        </li>
        <% if @user!=nil && @user.isAdmin %>

            <li>
              <%= link_to "Profile page", user_url(current_user.id) %>
            </li>
            <li>
              <%= link_to "Log Out", logout_path %>
            </li>
            <li>
              <%= link_to "Add", add_path(current_user.id) %>
            </li>
            <li>
              <%= link_to "Import", import_path(current_user.id) %>
            </li>
            <li>
              <%= link_to "Admin Page", users_path(current_user.id) %>
            </li>
        <% elsif current_user %>
            <li>
              <%= link_to "Profile page", user_url(current_user.id) %>
            </li>
            <li>
              <%= link_to "Log Out", logout_path %>
            </li>
        <% else %>
            <li>
              <%= link_to "Log In", login_path %>
            </li>
            <li>
              <%= link_to "Sign Up", signup_path %>
            </li>
        <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>

